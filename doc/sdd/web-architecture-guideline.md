# Web 架构规范（成熟模式）

适用目录：`web/`

## 推荐模式

1. 采用组件化架构 + 分层设计（`ui`、`application`、`domain`、`infrastructure`）。
2. 状态管理采用可预测模式（如单向数据流），避免隐式共享状态。
3. API 调用统一通过服务层封装，不在页面组件中散落请求逻辑。

## 强制规则

1. 页面组件不得直接耦合后端协议细节。
2. 业务规则必须沉淀到可复用模块（domain/application）中。
3. 新功能需包含关键交互与边界场景测试。
