# MienMien 贡献指南

感谢你对本项目的关注与贡献。

## CLA 强制要求（阻断合并）

所有 PR 在评审与合并前，必须具备有效 CLA 签署记录。

1. 阅读并签署 `CLA.md`。
2. 在 `doc/governance/cla-signatures/register.md` 填写签署登记，并在 PR 评论中附签署证据链接。
3. 等待 `cla-gate` 检查状态变为 `success`。

签署记录统一登记到 `doc/governance/cla-signatures/register.md`，并由维护者同步到 `.github/cla-signed-users.txt`。

若未签署 CLA，CI 将失败，PR 不可合并。

## 贡献流程

1. Fork 仓库并创建功能分支。
2. 在编码前完成当前需求的 SDD 变更文档（`doc/sdd/changes/<ChangeID>/vision-requirements.md`）。
3. 完成功能与测试。
4. 提交 PR 并补全 PR 模板。
5. 确保 CLA 检查、SDD 检查和其他必需检查全部通过。

## SDD 强制要求（阻断合并）

所有功能开发必须采用 SDD 流程。若 PR 未包含或未更新必要 SDD 文档，`sdd-gate` 检查将失败并阻断合并。

简化模式要求：

1. 每个新需求在 `doc/sdd/changes/` 下只创建一个变更文件夹（如 `REQ-0001`）。
2. 每个变更文件夹仅包含两个核心文件：
   - `vision-requirements.md`
   - `plan-task.md`
3. 必须先更新并确认 `vision-requirements.md`，再更新 `plan-task.md`。
4. 同一个 PR 仅允许关联一个变更文件夹，避免多需求混杂。

每次变更必须更新当前需求对应的变更目录下核心文件（`vision-requirements.md` 与/或 `plan-task.md`），并符合“先 vision 后 plan”的顺序约束。

## 许可与权利声明

提交贡献即表示你已知悉并同意：

1. 项目采用 BSL 1.1 许可，附加条款见 `LICENSE`。
2. 你的贡献受 `CLA.md` 约束，包括将贡献相关版权无偿授予 `王振`。

## 收益公开与贡献者回馈单方声明

以下声明由 `王振` 单方发布，不修改 `LICENSE` 或 `CLA.md` 的约束条款；该声明属于项目治理政策，不构成任何形式的劳动、合伙、证券、保底收益或其他法定给付承诺。

### 奖金周期与注入规则

1. 每年 `12月01日 00:00` 启动当年度奖金结算。
2. 当年度统计区间为：上一年度 `11月01日 00:00`（含）至本年度 `10月31日 00:00`（不含），即左闭右开区间。
3. 自统计区间核算出的 `MienMien` 当年度纯利润中，提取 `60%` 注入“本年度奖金池”。
4. 当年度奖金分配流程最晚于 `12月16日 23:59:59` 前结束。

### 奖金池分类与用途

1. 本年度奖金池：用于本年度贡献者奖金分配。
2. 历史奖金池：用于长期激励计划及相关活动（包括但不限于专项奖励、社区活动、人才培养计划）。

### 本年度奖金池分配规则

先决条件（强制）：

- 仅纳入满足以下全部条件的贡献者用户：
  1. 对应 PR 的创建时间在当年度统计区间内；
  2. 对应 PR 的合并时间在当年度统计区间内；
  3. 对应 PR 已完成合并（通过）。
- 任一条件不满足（含 PR 创建时间或合并时间超出统计区间）的用户，不纳入本年度奖金分配名单。
- 时间判定统一采用 UTC+8，且以代码托管平台记录字段（创建时间/合并时间）为唯一依据。

1. `30%`：平均分配给符合条件的贡献者。
2. `40%`：分配给 Top100 突出贡献者，采用“贡献权重点数”按比例分配。
3. `15%`：注入历史奖金池，作为滚存激励资金。
4. 剩余 `15%`：由项目方用于合规准备金、争议处理准备金、税费及支付通道成本等与奖金发放直接相关事项，并在年度披露中说明用途。

### Top100 比例分配的共识算法

为避免“名次数字越大反而分配越高”的歧义，Top100 分配采用以下统一口径：

1. 先按贡献评分从高到低生成 Top100 名单；
2. 设第 `i` 名贡献者的贡献权重点数为 `w_i`，且 `w_i > 0`；
3. Top100 总权重点数为 `W = Σw_i`；
4. 第 `i` 名贡献者奖金为：`Top100奖金总额 × (w_i / W)`；
5. 在默认排名权重模型下可取 `w_i = 101 - rank_i`（第1名权重100，第100名权重1）。

### 年度公开义务

项目方将按年度及时公开以下信息（可附延迟说明与审计注记）：

1. 当年度纯利润口径与金额；
2. 奖金池注入金额与分配明细；
3. 历史奖金池结余与当年使用明细；
4. `MienMien` 应用相关费用明细与年度简报（含主要成本类别与变化说明）。

上述文件统一归档于：

- `doc/governance/revenue-disclosures/`
- `doc/governance/bonus-distributions/`
- `doc/governance/cost-reports/`
- `doc/governance/cla-signatures/`

### 风险与边界说明

1. 实际发放金额以合法合规、税务处理、结算可达性与反洗钱/反欺诈审查结果为准。
2. 对于异常账户、违规贡献、争议贡献或身份无法核验情形，项目方有权暂缓发放并启动复核。
3. 贡献行为本身不当然产生所有权、版税、分红或其他财产性请求权。
4. 奖金政策为项目治理安排；在不违反适用法律的前提下，项目方可根据审计结论、监管要求与资金安全对执行细则作出必要调整，并在年度披露中说明。

### 安全漏洞响应时限（SLA）

1. 收到漏洞报告后，项目方应在 24 小时内完成收件确认。
2. 对高危/严重漏洞，项目方应在 72 小时内给出临时缓解方案或风险通告。
3. 漏洞修复与披露遵循“先修复后披露”原则，披露窗口原则上不早于修复上线后 7 天（特殊情形可延长并说明）。

## 联系方式

- 法务与 CLA 事项：`jijikeji2026@163.com`
- 安全漏洞报告：`jijikeji2026@163.com`

